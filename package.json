{
  "name": "@ggof/trestle",
  "version": "0.0.1",
  "description": "a tiny wrapper around `find-my-way`, a very fast and stdlib compliant router.",
  "author": "ggof",
  "license": "MIT",
  "keywords": [
    "http",
    "find-my-way",
    "web",
    "middleware",
    "filter"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dst/index.js",
    "dev": "pnpm build && pnpm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^18.16.3",
    "prettier": "^2.8.8",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "find-my-way": "^7.6.1"
  }
}
